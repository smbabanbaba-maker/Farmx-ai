# Agent Notes

This project is FarmX AI by SYLUTION LTD — a mobile-first agriculture assistant powered by Google Gemini and TanStack Start.

- AI key: `GEMINI_API_KEY` (server-only, set via environment variables).
- Payment key: `PAYSTACK_SECRET_KEY` (server-only, set via environment variables).
- Database: Supabase (client + server keys via environment variables).
- Do not hardcode secrets or API keys into the codebase.
